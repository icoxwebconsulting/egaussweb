    <!--breadcrumbs start-->
    <div id="breadcrumbs-wrapper" class="grey lighten-3">

        <div class="container">
            <div class="row">
                <div class="col s12 m12 l12">
                    <h5 class="breadcrumbs-title">Consejo de Administraci&oacute;n</h5>
                    <ol class="breadcrumb">
                        <li><a href="{{ path("dashboard") }}">Panel de Administraci&oacute;n</a>
                        </li>
                        <li><a href="#">Consejo de Administraci&oacute;n</a>
                        </li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!--breadcrumbs end-->


    <!--start container-->
    <div class="container">
        <p><a class="btn waves-effect waves-light indigo action" href="#" url="{{ path("_consejo_administracion_new") }}">Registrar</a> <a class="waves-effect waves-light btn action" href="#" url="{{ path("_consejo_administracion") }}">Atr&aacute;s</a></p>
        <div class="row">

        <div class="col s6">
            <div class="card" id="profile-card">
                <div class="card-image waves-effect waves-block waves-light">
                    <img alt="user background" style="opacity:0.1;" src="{{ asset("bundles/backend/images/eGauss_logo_head.png") }}" class="activator">
                </div>
                <div class="card-content">
                    <img class="circle responsive-img activator card-profile-image" alt="" src="{{ asset(entity.webPath) }}">
                    <a class="btn-floating activator btn-move-up waves-effect waves-light darken-2 right">
                        <i class="mdi-action-account-circle"></i>
                    </a>

                    <span class="card-title activator grey-text text-darken-4">{{ entity.name }} {{ entity.firstname }} {{ entity.lastname }}</span>
                    <p><i class="mdi-content-link cyan-text text-darken-2"></i> {{ entity.urlLinkedin }}</p>

                </div>
                <div class="card-reveal">
                    <span class="card-title grey-text text-darken-4">{{ entity.name }} {{ entity.firstname }} {{ entity.lastname }} <i class="mdi-navigation-close right"></i></span>
                    <p>{{ entity.descripcion }}</p>
                    <p><i class="mdi-content-link cyan-text text-darken-2"></i> {{ entity.urlLinkedin }}</p>
                </div>
            </div>
        </div>
        <div class="col s6">

        </div>
        </div>
        <div class="divider"></div>
    </div>

    <script>

        $(".action").on("click", function(e){
            $('.preloader-wrapper').addClass('active');
            var url = $(this).attr("url");
            $("#main-wrapper").load(url,function() {
                $('.preloader-wrapper').removeClass('active');
            });

        })

    </script>
